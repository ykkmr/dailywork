

-- 시퀀스를 사용하여 고정길이이며, 증가하는 숫자형식의 문자열을 반환하는 함수
-- 키로 사용하는 경우
-- SIST_0000000001의 형식으로 반환되어야한다

create or replace function sist_num
return char
is
	 temp_num varchar2(15);
begin

	 temp_num := concat('SiST_', lpad(seq_func.nextval,10,0));


	 return temp_num;

end;
/
